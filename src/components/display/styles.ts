import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  display: {
    color: '#fff',
    fontSize: 60,
    textAlign: 'right',
    marginRight: 30,
    marginBottom: 10,
  },
  displayLandscape: {
    color: '#fff',
    fontSize: 40,
    textAlign: 'right',
    marginRight: 30,
    marginBottom: 0,
  },
});

export default styles;
